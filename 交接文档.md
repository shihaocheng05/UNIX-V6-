# UNIX V6++ 虚拟存储器项目交接文档

*最后更新：2026年6月18日*

---

## 一、项目状态

在离散化+COW改造基础上，实现了请求调页式虚拟存储器。系统可启动，Shell 可运行。**`ls` 命令可正常执行**，但执行其他程序（如 `mkdir`、`echo`、`cat`）后出现磁盘数据损坏，后续所有命令（包括 `ls`）均不可用。

---

## 二、现象描述

### 2.1 当前症状（Bug 1/2 修复后，2026-06-18 调试输出）

测试序列：`ls` → `mkdir /h` → `ls` → `mkdir /h`

**第一次 ls**：正常输出目录列表（`bin  demos  etc  Shell.exe` 等）。

**第一次 mkdir**：
- Shell fork 子进程（pid=2），exec `/bin/mkdir`，成功创建目录 `/h`，进程退出。

**第二次 ls**：仍然正常，目录列表中包含新建的 `/h`。

**第二次 mkdir**：
- Shell fork 子进程（pid=3），exec `/bin/mkdir` 成功（`Exec done: pid=3 stat=3 sig=0`）。
- 文本段缺页加载时，ReadI 返回第一字为 `0x00000000`（非 x86 机器码）。
- 进程加载到无效代码后崩溃，触发 Kernel Page Fault。

### 2.2 对比数据

| 时间点 | ReadI 第一字 | 第二字 | 来源 |
|--------|-------------|--------|------|
| Shell 文本加载（正常） | `0x83E58955` | `0x458B18EC` | inode=6518, offset=0x400 |
| 第二次 mkdir 文本加载（异常） | `0x00000000` | `0x005F0044` | inode=6493, offset=0x400 |

`0x83E58955` 小端字节为 `55 89 E5 83`，即 `push ebp; mov ebp, esp`，合法 x86 代码。
`0x00000000` 为 NULL，`0x005F0044` 字节为 `44 00 5F 00`，呈现目录项数据特征。

同一 inode（6493，`/bin/mkdir`）、同一文件偏移（0x400），在不同时间点读到不同内容，说明磁盘上 `/bin/mkdir` 的数据块已被覆写。

---

## 三、已确认的配置参数

| 参数 | 值 | 来源 |
|------|-----|------|
| c.img 大小 | 10080 KB = 20160 块（512B/块） | 文件属性 |
| `s_fsize` | 20160 | Diagnose::Write 输出 |
| 交换区起始块 | 18000 | SwapperManager.h |
| 交换区块数 | 2000（250 页 × 8 块/页） | SwapperManager.h |
| 交换区范围 | 块 18000 – 19999 | 计算 |
| 文件系统空闲块范围 | 块 0 – 20159（受 s_fsize=20160 控制） | FileSystem::Alloc |

**确认：文件系统空闲块链与交换区存在空间重叠。** s_fsize=20160 使得文件系统可将块 18000–19999 分配给文件数据，同时 SwapperManager 也将同一批块号用于页面交换和 u 结构存储。

---

## 四、已修复的 Bug

### 4.1 离散化+COW 阶段

| Bug | 文件 | 修复 |
|-----|------|------|
| CopyUserPageTable 仅对 RW=1 页做计数 | MemoryDescriptor.cpp | 改为对所有 Present=1 页计数 |
| CopyUserPageTable 对正文段也做 COW 计数 | MemoryDescriptor.cpp | 用 isText 排除 text+rdata |
| Exec 栈分配使用旧 m_StackSize | ProcessManager.cpp | 使用 parser.StackSize |
| Exit 中 PPDA 页未释放 | Process.cpp | 增加 FreeMemory(p_addr) |
| Exec 末尾 IPut 提前释放 inode | ProcessManager.cpp | 删除 IPut |

### 4.2 VM 阶段（代码排查+修复）

| Bug | 文件 | 修复 |
|-----|------|------|
| FreeSwapList 结构体末尾缺分号 | Page.h | 补分号 |
| current 变量未声明 | MemoryDescriptor.h | 添加成员 |
| EstablishUserPageTable stack 索引用错 | MemoryDescriptor.cpp | heapVirtualIdx→stackVirtualIdx |
| f_offset 计算错用左移代替除法 | Exception.cpp | 改为 `/4096*4096` |
| ReadI 目标地址用 virtualIdx | Exception.cpp | 改为 `cr2&0xFFFFF000` |
| Victim PTE 更新缺失 | ProcessManager.cpp | 添加 Present=0+PageBaseAddress |
| rdataPageNum 计算用 v_start 代替 v_length | MemoryDescriptor.cpp | 改用 v_length |
| FreePhyPage m_Used 设反 | MemoryDescriptor.cpp | 改为 `=0` |
| FreePhyPage 交换区编码不一致 | MemoryDescriptor.cpp | 统一传 m_PageBaseAddress |
| selectVictim 取模顺序 | MemoryDescriptor.cpp | 先取模再访问 |
| Exit 分块写入 | Process.cpp | 改为 8×512B 循环 |
| Radix tree root slot 未初始化 | RadixTreeNode.cpp | 构造函数初始化 |
| FreeRangePage 循环条件导致泄漏 | RadixTreeNode.cpp | 修复循环条件 |
| EstablishUserPageTable 缺 FlushPageDirectory | MemoryDescriptor.cpp | 末尾加 Flush |
| BSS 清零用 virtualIdx 代替实际虚拟地址 | Exception.cpp | 改为 `cr2&0xFFFFF000` |
| IPut 中 radix tree 清理位置修正 | OpenFileManager.cpp | 移到 i_nlink 条件外 |
| ExecShell 在 ring3 执行前缺栈页 | main.cpp | 预先分配映射栈页 |

### 4.3 本轮排查修复

| Bug | 文件 | 修复内容 |
|-----|------|---------|
| Exec 预分配 data 首页未从 PE 文件加载 | ProcessManager.cpp:663-666 | 增加 `pInode->ReadI()` 加载 .data 初始值 |
| Exec 预分配 BSS 首页未清零 | ProcessManager.cpp:679-682 | 增加循环将 BSS 页全部写零 |

---

## 五、已识别但未修复的代码 Bug

| 优先级 | Bug | 文件 | 说明 |
|--------|-----|------|------|
| P1 | data 段 Path 1 加载后未将 PTE 回设为 RO | Exception.cpp:418-441 | 共享缓存页被进程直接写入污染，后续 exec 同程序时拷贝到脏数据。text 段正确做了 `m_ReadWriter=0` |
| P1 | selectVictim 第二圈可能无限循环 | MemoryDescriptor.cpp:369-393 | 所有 RW 页 COW 计数 >1 时永久 `continue` |
| P2 | Exec 错误路径 sectionHeaders 泄漏 | ProcessManager.cpp:387-391 | HeaderLoad 失败后直接 return 未释放 |
| P2 | Exec 中 userPageTableArray 变量名遮蔽 | ProcessManager.cpp:421 vs 488 | 内层声明遮蔽外层同名变量（功能正确但易误导） |

---

## 六、已尝试但未解决问题的操作

1. 在 `FileSystem::Alloc` 中添加 BadBlock 检查，阻止文件系统分配交换区块（18000–19999）。测试结果：问题依旧。
2. 修复 Bug 1（data 页加载）和 Bug 2（BSS 页清零）。测试结果：问题依旧。

---

## 七、当前排查方向

第二次 `mkdir` exec 时文本段 ReadI 返回无效数据（`0x00000000`）。该数据在第一次 `mkdir` exec 时尚为正确值。磁盘变更发生在第一次 `mkdir` 的 Exit 路径中。

Exit 路径中文本 inode 的 IUpdate 将内存 inode 写回磁盘。需确认：**在 IUpdate 执行前，内存中 inode 6493 的 `i_addr[]` 是否已被破坏。**

建议在 `OpenFileManager.cpp` 的 `IPut` 函数中，`FreeRangePage` 调用前后添加诊断：

```cpp
Diagnose::Write("Before FreeRange: ino=%d addr[0]=%x addr[1]=%x addr[2]=%x\n",
    pNode->i_number, pNode->i_addr[0], pNode->i_addr[1], pNode->i_addr[2]);
radix_tree_node::FreeRangePage(inode_layer_node);
Diagnose::Write("After FreeRange: ino=%d addr[0]=%x addr[1]=%x addr[2]=%x\n",
    pNode->i_number, pNode->i_addr[0], pNode->i_addr[1], pNode->i_addr[2]);
```

目的：确认 `FreeRangePage` 调用的 `kAllocator.FreeMemeory`（内核堆释放）是否破坏了相邻的 Inode 表内存。

---

## 八、关键代码位置

| 功能 | 文件 | 行号 |
|------|------|------|
| 缺页异常处理 | src/interrupt/Exception.cpp | 254–554 |
| Fork（NewProc） | src/proc/ProcessManager.cpp | 62–145 |
| Exec | src/proc/ProcessManager.cpp | 345–693 |
| Exit | src/proc/Process.cpp | 127–259 |
| COW 页表复制 | src/proc/MemoryDescriptor.cpp | 228–254 |
| 物理页释放 | src/proc/MemoryDescriptor.cpp | 287–330 |
| Clock 页面选择 | src/proc/MemoryDescriptor.cpp | 334–384 |
| IPut（含 FreeRangePage + IUpdate） | src/fs/OpenFileManager.cpp | 207–248 |
| IUpdate（inode 写回磁盘） | src/fs/INode.cpp | 484–537 |
| Radix Tree 操作 | src/mm/RadixTreeNode.cpp | 1–130 |
| UserPageManager | src/mm/PageManager.cpp | 111–181 |
| SwapperManager | src/mm/SwapperManager.cpp | 1–63 |
| 系统调用入口 | src/interrupt/SystemCall.cpp | 91–141 |

---

## 九、调试诊断位置

| 诊断内容 | 文件 | 位置 |
|---------|------|------|
| s_fsize 值 | ProcessManager.cpp | Exec 入口处 |
| Post-ReadI verify | Exception.cpp | text/rdata 缺页处理后 |
| ReadI inode/fileOffset/dest/size | Exception.cpp | Post-ReadI verify 之后 |
| pageNo + offset3F0 | Exception.cpp | ReadI 诊断之后 |

---

## 十、磁盘恢复方法

```bash
copy D:\UNIX V6++test\oos\tools\v6pp-fs-edit-2022\workspace\c.img D:\UNIX V6++test\oos\targets\UNIXV6++\c.img
```
