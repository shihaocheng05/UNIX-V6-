# UNIX V6++ 虚拟存储器项目交接文档

*文档生成时间：2026年6月*

---

## 一、项目概述

为 UNIX V6++ 操作系统实现**请求调页式虚拟存储器**，在现有离散化+COW改造的基础上，增加：
- 请求调页（demand paging）：代码/数据段不预分配物理页，缺页时惰性加载
- vm_area 虚拟段管理：每个逻辑段（text/data/rdata/bss/stack/heap）一个控制块
- Radix Tree 共享页缓存：文件映射页（text/rdata）的全局缓存
- Clock 局部页面置换：内存不足时淘汰私有页到交换区
- IPut 时释放 radix tree 中该 inode 的所有缓存页

---

## 二、当前工作分支

- **分支名**：`vm`
- **工作目录**：`D:\UNIX V6++test\oos\`

---

## 三、已修复的 Bug（离散化+COW 阶段）

| Bug | 文件 | 修复内容 |
|-----|------|---------|
| CopyUserPageTable 仅对 RW=1 页做计数 | `MemoryDescriptor.cpp` | 改为对所有 Present=1 页计数 |
| CopyUserPageTable 对正文段也做 COW 计数 → Page[] 泄漏 | `MemoryDescriptor.cpp` | 用 isText 检查排除 text+rdata 段 |
| Exec 栈分配使用旧 m_StackSize | `ProcessManager.cpp` | 使用 parser.StackSize |
| Exit 中 PPDA 页未释放 | `Process.cpp` | 增加 FreeMemory(p_addr) |
| Exec 末尾 IPut 提前释放 inode | `ProcessManager.cpp` | 删除 IPut，inode 保持有效供缺页使用 |

---

## 四、新引入/修改的文件清单

### 4.1 新文件

| 文件 | 内容 |
|------|------|
| `src/include/Page.h` | `struct page`（20B）、`FreeList`、`SwapPage`、`FreeSwapList` |
| `src/include/VMArea.h` | `struct vm_area`（虚拟段控制块） |
| `src/include/RadixTreeNode.h` | `class radix_tree_node`（共享页缓存树，256路分叉，高度0-3） |
| `src/mm/RadixTreeNode.cpp` | 实现 `FreeRangePage`、`FindLeafNode`、`EstablishPathToTrueLeaf` |
| `docs/code-review-vm.md` | 代码静态排查报告（修订版） |
| `docs/vm.md` | 设计改动说明文档 |

### 4.2 修改的文件

| 文件 | 关键改动 |
|------|---------|
| `PageTable.h` | PTE 新增 `m_Used` 位（区分 NULL PTE 和已换出 PTE）；`m_Present` 语义改为 1=内存/0=磁盘 |
| `PageManager.h` | `UserPageManager`：新增 `freeList`、`pages[]` 静态数组、`sharedPageRoot` radix tree 根节点；`AllocMemory/FreeMemory`改为无参/单参 |
| `PageManager.cpp` | `UserPageManager` 构造/初始化/Alloc/Free 实现 |
| `Allocator.h` | 保留 `Page[]` 数组仅作引用计数 |
| `SwapperManager.h` | 新增 `SwapperPage` 数组、`freeSwapList`；`AllocSwap/FreeSwap` 改为无参/单参 |
| `SwapperManager.cpp` | 实现页级交换区分配/释放 |
| `INode.h` | 新增 `i_index` 字段（在 m_Inode 数组中的下标） |
| `Text.h` | 删除 `x_ccount`、`x_daddr`、`x_caddr`；仅保留 `x_count`、`x_iptr`、`x_pgTable`、`x_size` |
| `Text.cpp` | XFree 逻辑简化，x_count→0 时逐页释放 |
| `User.h` | 新增 `vm_area vm_list[VM_AREA_MAX]`，hardcode 段索引 |
| `MemoryDescriptor.h` | 新增 `current`（Clock 指针）、`FreePhyPage`、`selectVictim`、`CopyUserPageTable` 签名变更 |
| `MemoryDescriptor.cpp` | 实现 `EstablishUserPageTable`（仅初始化 PTE 为 NULL）、`FreePhyPage`（释放页并更新交换区）、`selectVictim`（Clock 算法）、`CopyUserPageTable`（COW 排除 text） |
| `ProcessManager.cpp` | Exec 重构：仅分配栈页+参数，其他段不分配；Sched 删除整进程换入/换出；NewProc 新增 swap out 流程 |
| `Process.cpp` | Exit 使用 `FreePhyPage` 释放各段；PPDA 释放；text 段处理简化 |
| `PEParser.h` | 新增 RDataSize、RDataAddress、BSSAddress、BSSSize 成员 |
| `Exception.cpp` | PageFault 处理程序：根据 vm_area 类型分发，text/rdata→radix tree，data→COW+文件加载，bss/stack/heap→清零或 COW |
| `OpenFileManager.cpp` | `IGet` 设置 `i_index`；`IPut` 增加 radix tree 清理 |
| `main.cpp` | Sched 替换为 Idle 空循环 |

---

## 五、核心数据结构

### 5.1 PTE 状态编码

```
m_Present   m_Used   m_PageBaseAddress   →  含义
    1          1        物理页框号        在内存中
    0          1        交换区块号        在磁盘交换区
    0          0        0                 从未分配（NULL PTE）
    1          0        —                 （不允许存在）
```

### 5.2 Radix Tree 密钥

```
Key (32-bit unsigned int):
  bit 31-24:  inode 在 m_Inode[] 中的下标（0-99，由 Inode.i_index 提供）
  bit 23-0:   文件页号偏移（支持 16M×4KB = 64GB 文件）

树结构（256 路分叉）:
  ROOT (height=3) → INODE 层 (height=2) → FIRST 层 (height=1) → LEAF 层 (height=0)
  每层 256 个 slot，叶子 slot 指向 struct page
```

### 5.3 Page[] 与 struct page 分工

```
Page[frame] (unsigned int):  COW 引用计数（Fork+1, FreeMemory-1, 归零则回 freelist）
struct page:                元数据（inode_id, pageOffset, pageNo, next）
                              挂在 freelist（空闲）或 radix tree 叶子（共享页缓存）
```

### 5.4 交换区编码

```
AllocSwap 返回: SWAPPER_ZONE_START_BLOCK + pageNo * 8  (完整块号)
m_PageBaseAddress (Present=0): 完整块号
FreeSwap(startBlock): (startBlock - SWAPPER_ZONE_START_BLOCK) / 8 → 得到 pageNo
```

---

## 六、待修复的剩余 Bug（P0/P1）

详见 `docs/code-review-vm.md`：

| 优先级 | 问题 | 位置 |
|--------|------|------|
| **P0** | rdataPageNum 计算公式用 v_start 代替 v_length | `MemoryDescriptor.cpp:237` |
| **P0** | FreePhyPage m_Used 设为 1 而非 0 | `MemoryDescriptor.cpp:308` |
| **P0** | 交换区编码不一致：FreePhyPage 多乘了 8 | `MemoryDescriptor.cpp:305,325` |
| **P1** | Page[0x401] 在 UserPageManager 构造中被误清零 | `PageManager.cpp:115` |
| **P1** | selectVictim 取模在越界访问之后 | `MemoryDescriptor.cpp:341-343` |
| **P1** | selectVictim 可能无限循环 | `MemoryDescriptor.cpp:357-381` |
| **P1** | Swap 读回地址用 virtualIdx 而非 cr2 | `Exception.cpp:388,489` |

---

## 七、设计决策记录

1. **vm_area 放 User 静态数组**：每进程最多 8 段（~192 字节），避免内核堆碎片。段索引 hardcode（TEXT=0, DATA=1, RDATA=2, BSS=3, HEAP=4, STACK=5）。

2. **正文段生命周期**：x_count 管理 Text 结构体去留，Page[] 管理物理页框去留。CopyUserPageTable 排除 text+rdata 段。

3. **共享页（text/rdata）不参与 Clock 淘汰**：只淘汰 `Page[count]==1` 的私有页。共享页通过 IPut→FreeRangePage 整树清理。

4. **PPDA 永不换出**：可运行进程的 PPDA 必须在内存中（SwtchUStruct 要求物理地址）。

5. **0# 进程 Sched → Idle**：删除整进程换入/换出，替换为 Sleep 空循环。

6. **脏页标记**：使用硬件 PTE Dirty 位，软件不额外维护。干净页（text/rdata）直接丢弃→inode 重读；脏页写交换区。

7. **内核区/用户区物理页分层**：内核区（4MB以下）急式分配、永不换出；用户区（4MB以上）惰性分配、freelist 管理、可换出。

8. **IPut 时清理 radix tree**：inode 引用归零时调用 FreeRangePage 释放所有属于该 inode 的缓存页框。

---

## 八、关键调用链速查

```
Fork 创建子进程:
  Fork() → NewProc()
    ├── AllocMemory() → 分配 PPDA
    ├── (若失败) selectVictim() → Clock 淘汰 → 页写交换区 → 更新 victim PTE
    ├── CopyUserPageTable() → COW 复制（排除 text+rdata）
    └── CopySeg(PPDA)

Exec 加载程序:
  Exec()
    ├── FreePhyPage(旧栈) / FreePhyPage(旧数据)
    ├── AllocMemory() → fakeStack 一页
    ├── 构建 argc/argv 到栈
    ├── FreePhyPage(旧 text, x_count→0 时)
    ├── Text 结构体查找/创建
    ├── EstablishUserPageTable() → PTE 置 m_Used=0
    └── (不再调用 Relocate，不再 IPut)

缺页异常 (INT 14):
  PageFault(cr2)
    ├── 遍历 vm_list 确定 vm_id
    ├── text/rdata → radix tree 查找/加载 → 建立 PTE（RO）
    ├── data → COW 或 inode 加载 → 建立 PTE（RW）
    └── bss/stack/heap → 清零页 或 COW → 建立 PTE（RW）

进程退出:
  Exit()
    ├── FreePhyPage(各段) → Page[]--, 归零回 freelist
    ├── 释放 PPDA
    ├── x_count-- → 若归零 → FreePhyPage(text) + IPut(inode)
    └── IPut(inode) → FreeRangePage(radix tree) → 释放所有缓存页
```

---

*上一个版本：2026年6月（初始版）*
*本次更新：新增剩余 Bug 清单、设计决策记录、调用链速查、修订代码排查报告*
